#include <stdio.h>

//Escreva uma função que devolva o índice de um determinado valor num array

int main()
{
    int indice;
    int rei;
    int arrei[] = (23, 356, 32, 1, 75, 12, 424, 52, 31, 2); 
    
    printf("Qual e o numero que quer encontrar? ");
    scanf("%d",&rei);
    for (int i = 0; i <= 9; i++)
    {
        if ()
        {

        }
        
    }

    printf("O numero %d esta no indice %d", rei, indice);

    return 0;
}
